"""Module 1 (partial) — Desktop UI shell (PySide6).

This package is the ONLY place Qt is imported. Domain modules never depend on it.
Importing this package requires the ``ui`` extra (``pip install -e ".[ui]"``).
"""
