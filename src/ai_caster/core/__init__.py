"""Core cross-cutting infrastructure: logging, paths, the event bus and the
shared interfaces every module is built on. This package has no dependency on
any feature module and never imports UI code.
"""
